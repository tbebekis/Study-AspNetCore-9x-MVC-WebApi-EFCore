@model ProductModel

<div class="page">

    <div class="page-caption">
        <div class="page-title">@L("Product.Edit")</div>
    </div>

    @await Html.PartialAsync("_InsertOrEdit", Model)


    @*
    <form asp-route="Product.Save" method="post">
        @await Html.PartialAsync("_InsertOrEdit", Model)
    </form>
    *@

</div>